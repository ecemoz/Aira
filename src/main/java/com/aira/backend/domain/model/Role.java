package com.aira.backend.domain.model;

public enum Role {
    USER, ADMIN
}
// Simple and scalable role-based access control.
// More roles (like MODERATOR, ANALYST etc.) can be added easily in the future.